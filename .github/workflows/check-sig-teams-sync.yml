# Generated with Claude Code
name: Check SIG_TEAMS Sync

on:
  pull_request_target:
    paths:
      - 'tests/**/OWNERS'
      - 'SIG_TEAMS'

permissions:
  contents: read

jobs:
  check-sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true

      - name: Check SIG_TEAMS sync
        run: uv run python .github/scripts/check_sig_teams_sync.py
